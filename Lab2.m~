% [XTrain, YTrain, XValidation, YValidation, XTest, YTest] = load_data;

methods = {'RandRotation', 'RandXTranslation', 'RandYTranslation', 'RandYReflection', 'RandXReflection'};
% {[-20,20], [-3 3], [-3 3], true, true}
imageAugmenter_cont = containers.Map(methods,{1,2,3,4,5});
keys = {'transform', 'low-res', 'soften', 'low-light'};
imgs = containers.Map(keys, {img1, img3, img4, img5});

keys = keys(methods);
val = values(imageAugmenter_cont) ;

% Initialize
map = containers.Map('RandRotation', 'RandXTranslation', 'RandYTranslation', 'RandYReflection', 'RandXReflection');



for i = 1:length(imageAugmenter_cont)
    
end
for k = keys(imageAugmenter_cont)
    imageAugmenter = imageDataAugmenter(k,imageAugmenter_cont{k});
end

imageSize = [28 28 1];
augimds = augmentedImageDatastore(imageSize,XTrain,YTrain,'DataAugmentation',imageAugmenter);